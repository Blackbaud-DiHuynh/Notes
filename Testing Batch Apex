Testing Batch Apex

Setting: 
	- System.abortJob(batchableContext.getJobId())
	- callouts 

Hurdle 1 - BatchableContext
	- start(Database.BatchableContext bc){
		return start(bc.getJobId()); 
	}


Hurdle 2 - System.abortJob()
	- abortJob(jobId)
		if (!Test.isRunningTest()){
			System.abortJob(jobId);
		}

Hurdle 3 - callouts

